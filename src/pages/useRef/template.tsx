import * as React from 'react';
import { HomeButton } from '../../components/atoms/HomeButton';

export const UseRefPage: React.FC = () => {
  return (
    <div>
      <HomeButton />
      <h1>useRef</h1>
    </div>
  );
};
